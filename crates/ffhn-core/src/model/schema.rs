/// Canonical schema name for `ffhn.target`.
pub const TARGET_SCHEMA_NAME: &str = "ffhn.target";
/// Canonical schema version for `ffhn.target`.
pub const TARGET_SCHEMA_VERSION: u32 = 1;

/// Canonical schema name for `ffhn.extraction_record`.
pub const EXTRACTION_RECORD_SCHEMA_NAME: &str = "ffhn.extraction_record";
/// Canonical schema version for `ffhn.extraction_record`.
pub const EXTRACTION_RECORD_SCHEMA_VERSION: u32 = 1;

/// Canonical schema name for `ffhn.state`.
pub const STATE_SCHEMA_NAME: &str = "ffhn.state";
/// Canonical schema version for `ffhn.state`.
pub const STATE_SCHEMA_VERSION: u32 = 1;

/// Canonical schema name for `ffhn.run_report`.
pub const RUN_REPORT_SCHEMA_NAME: &str = "ffhn.run_report";
/// Canonical schema version for `ffhn.run_report`.
pub const RUN_REPORT_SCHEMA_VERSION: u32 = 1;

/// Canonical schema name for `ffhn.batch_run_report`.
pub const BATCH_RUN_REPORT_SCHEMA_NAME: &str = "ffhn.batch_run_report";
/// Canonical schema version for `ffhn.batch_run_report`.
pub const BATCH_RUN_REPORT_SCHEMA_VERSION: u32 = 1;

/// Canonical schema name for `ffhn.status_report`.
pub const STATUS_REPORT_SCHEMA_NAME: &str = "ffhn.status_report";
/// Canonical schema version for `ffhn.status_report`.
pub const STATUS_REPORT_SCHEMA_VERSION: u32 = 1;

/// Canonical HTMLCut interop profile consumed by FFHN v2.
pub const HTMLCUT_INTEROP_PROFILE: &str = INTEROP_V1_PROFILE;
use htmlcut_core::interop::v1::INTEROP_V1_PROFILE;
