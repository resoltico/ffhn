# Exclude maintainer-only agent and automation config from `git archive`
# source packages so the files can stay versioned in Git without shipping
# inside FFHN-owned release assets.
/AGENTS.md export-ignore
/.codex/** export-ignore
/.github/** export-ignore
