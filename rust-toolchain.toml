[toolchain]
# Rust 1.97.0 is the maintained FFHN toolchain for development, linting, tests,
# and release builds. We install nightly separately only for branch coverage, because
# `cargo llvm-cov --branch` currently requires it.
channel = "1.97.0"
components = ["clippy", "rustfmt", "llvm-tools-preview"]
