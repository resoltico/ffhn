[workspace]
members = [
    "crates/ffhn-core",
    "crates/ffhn-cli",
    "xtask",
]
exclude = ["semver-baseline/ffhn-core"]
resolver = "3"

[workspace.package]
version = "7.0.0"
edition = "2024"
rust-version = "1.95"
description = "Deterministic monitoring for websites and local HTML files with repeatable checks and JSON reports"
license = "MIT"
authors = ["Ervins Strauhmanis"]
repository = "https://github.com/resoltico/ffhn"
homepage = "https://github.com/resoltico/ffhn"

[workspace.dependencies]
assert_cmd = "2.2.1"
clap = { version = "4.6.1", features = ["derive"] }
encoding_rs = "0.8.35"
ffhn-core = { path = "crates/ffhn-core", version = "7.0.0" }
fs2 = "0.4.3"
htmlcut-core = { version = "10.0.0", git = "https://github.com/resoltico/HTMLCut.git", tag = "v10.0.0", package = "htmlcut-core" }
predicates = "3.1.4"
regex = "1.12.3"
scraper = "0.27.0"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
sha2 = "0.11.0"
tempfile = "3.27.0"
thiserror = "2.0.18"
time = { version = "0.3.47", features = ["formatting", "parsing"] }
toml = "1.1.2"
ureq = { version = "3.3.0", default-features = false, features = ["gzip", "platform-verifier", "rustls"] }
url = { version = "2.5.8", features = ["serde"] }

[workspace.lints.rust]
unsafe_code = "deny"
unused_must_use = "deny"
unsafe_op_in_unsafe_fn = "deny"

[workspace.lints.clippy]
all = "warn"
cast_possible_truncation = "warn"

[profile.dist]
inherits = "release"
lto = "thin"
codegen-units = 1
strip = "symbols"
panic = "abort"

[profile.dev]
incremental = false

[profile.test]
incremental = false
